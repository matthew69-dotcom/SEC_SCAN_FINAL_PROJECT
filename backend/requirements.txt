# Versions chosen to have prebuilt wheels for Python 3.12 AND 3.13.
fastapi>=0.115.6
uvicorn[standard]>=0.32.0
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
httpx>=0.28.1
dnspython>=2.7.0
cryptography>=44.0.0
pyyaml>=6.0.2
slowapi>=0.1.9
# Dev
pytest>=8.3.4
pytest-asyncio>=0.24.0
ruff>=0.8.4
# AI (Week 6) — required for the AI Risk Analyzer.
# Without these, AI silently falls back to a local template (generated_by_ai=False).
langchain>=0.3
langchain-openai>=0.2
langchain-core>=0.3
# NOTE: sqlalchemy + asyncpg to be added Week 7 (PostgreSQL — Role C).
